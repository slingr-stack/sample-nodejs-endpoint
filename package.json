{
  "name": "sample",
  "version": "1.0.0",
  "description": "Sample Endpoint for creating Slingr Endpoints",
  "main": "index.js",
  "scripts": {
    "start": "node endpoint.js",
    "startDev": "nodemon endpoint.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/slingr-stack/sample-nodejs-endpoint"
  },
  "keywords": [
    "slingr",
    "sample",
    "endpoints"
  ],
  "dependencies": {
    "nodemon": "^2.0.15",
    "slingr-endpoints": "^1.2.0"
  }
}
